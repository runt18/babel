function multilineTemplate() {
  return `I\'m done reconfoobling\n${ 'the energy motron' }\n      ${ '...or whatever' }`;
}

{
  const foo = `spam\nand eggs!`;

  const bar = `${ 4 + 2 }`;

  const hello = `Hello\n${ 'world' }`;
}
